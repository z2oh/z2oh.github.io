title: misc
layout: default.liquid
data:
    route: misc
---
<ul class="project-list main-list">
    {% for post in collections.posts.pages %}
        {% if post.data.type == "misc" %}
            <li>
                <a href="{{ post.permalink }}"><h3>{% case post.data.link_type %}{% when "external" %}[➚] {% when "pdf" %}[PDF] {% endcase %}{{ post.title }}</h3></a>
                <p>{{ post.description }}</p>
                <hr>
            </li>
        {% endif %}
    {% endfor %}
</ul>
